server:
  port: 1006

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/generalinsurance
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update